import * as React from 'react';

const AdminPage = (props) => {
    const {
      classes,
    } = props;
    return (
      <div className={classes.root}>
      </div>
    );
  };
  
  
  export default AdminPage;