import * as React from 'react';

const Register = (props) => {
    const {
      classes,
    } = props;
    return (
      <p style={{color:'#434343'}}>Sign Up</p>
    );
  };
  
  
  export default Register;