import * as React from 'react';

const Following = (props) => {
    const {
      classes,
    } = props;
    return (
      <div className={classes.root}>
      </div>
    );
  };
  
  
  export default Following;