import * as React from 'react';

const Sidemenu = (props) => {
    const {
      classes,
    } = props;
    return (
      <div className={classes.root}>
      </div>
    );
  };
  
  
  export default Sidemenu;