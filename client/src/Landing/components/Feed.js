import * as React from 'react';

const Feed = (props) => {
    const {
      classes,
    } = props;
    return (
      <div className={classes.root}>
      </div>
    );
  };
  
  
  export default Feed;