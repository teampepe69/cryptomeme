import * as React from 'react';

const Login = (props) => {
    const {
      classes,
    } = props;
    return (
        <p style={{color:'#434343'}}>Log In</p>
    );
  };
  
  
  export default Login;