import * as React from 'react';

const Banner = (props) => {
    const {
      classes,
    } = props;
    return (
      <div className={classes.root}>
      </div>
    );
  };
  
  
  export default Banner;