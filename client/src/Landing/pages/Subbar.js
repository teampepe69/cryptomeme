import * as React from 'react';

const Subbar = (props) => {
    const {
      classes,
    } = props;
    return (
      <p style={{color:'#434343'}}>Subbar Stuff</p>
    );
  };
  
  
  export default Subbar;